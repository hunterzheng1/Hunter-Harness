# Hunter Harness 手工测试反馈

本文件记录 2026-08-08 本地联调期间确认的问题、方案与实施结果。Owner 已于同日确认统一修复；以下项目已进入 `0.2.55` 发布候选，最终状态以发布和本地联调结果为准。

## HH-MT-001：初始化后未维护 Harness 生成文件的 Git 忽略策略

- 状态：已修复，待发布与本地验收
- 类型：产品体验 / 仓库卫生
- 范围：`hunter-harness` CLI 初始化、刷新、平台绑定
- 现象：运行 `npx hunter-harness` 后会生成 `.harness`、Agent skill/rule 投影和指令文档，但首次安装不会创建或补全 `.gitignore`。目前只有 `connect` 会确保 `.harness/credentials.local.yaml` 与 `.harness/credentials.local.*` 被忽略。
- 影响：消费仓会出现大量与业务源码无关的未跟踪文件；用户容易误提交运行状态、缓存、归档、worktree 或可重新生成的 Agent 适配文件。

### 实施结果（0.2.55）

初始化现在会创建或补全 `.gitignore`，仅加入 Harness 拥有的本地状态、worktree 和 `harness-*` 适配文件规则。根指令文档通过本地来源记录区分“安装前已有”和“CLI 新建”，只有 CLI 新建且项目成功绑定平台时才默认忽略；已跟踪文件、已有内容、CRLF 和显式反向规则均保留。dry-run 与 JSON 输出会把 `.gitignore` 纳入 planned/applied 清单。

### 结论

这是需要优化的真实问题，但不能把 `.codebuddy/`、`.claude/`、`.cursor/` 等第三方目录整体忽略，因为其中可能已有用户维护的设置、规则或技能。应按 Hunter Harness 的文件所有权追加精确规则。

建议默认忽略：

- 本地状态与执行产物：`/.harness/`、`/.worktrees/`，以及各 Agent 的 `worktrees/` 子目录。
- Harness 生成的适配器投影：各 Agent 目录下名称为 `harness-*` 的 skills、agents 和 rules；不得覆盖或忽略同目录中的用户文件。
- Python/测试运行缓存继续按现有生成缓存策略排除。

根目录 `AGENTS.md`、`CLAUDE.md`、`CODEBUDDY.md` 需要所有权保护：

- 安装前已存在或已被 Git 跟踪时，视为项目文件，不自动加入忽略列表。
- 由 CLI 新建且项目已成功绑定 Hunter Platform 时，可默认采用“平台托管、不提交 Git”；仍应允许用户选择“随源码提交”，因为这些文档也承担团队协作、分支审阅和无平台环境下的项目规则入口。
- 项目未绑定平台时，不应无提示地把唯一一份自定义指令排除在 Git 和远端备份之外。

### 预期实现与验收

1. 首次安装完成时：没有 `.gitignore` 就创建；已有文件则保留原内容、编码和换行，只补缺失规则。
2. 刷新和后续安装时保持幂等，不重复写入规则或注释段。
3. 尊重已有的显式反向规则（`!path`），不悄悄覆盖用户的 Git 策略。
4. 通过 `git check-ignore` 或等价匹配确认规则是否已经生效，而不是只检查完全相同的文本行。
5. 对已经被 Git 跟踪的生成文件只报告清单和处理建议；不得自动执行 `git rm --cached`、修改暂存区或删除文件。
6. `.codebuddy/`、`.claude/`、`.cursor/`、`.agents/` 只追加 Harness 所有的精确子路径，不整目录忽略。
7. `--dry-run`、`--json` 和非交互模式需要返回确定的 planned/applied/skipped 结果，并覆盖无 Git 仓、空 `.gitignore`、已有广义规则、negation、CRLF、重复执行和已跟踪文件测试。

## HH-MT-002：CLI 首页项目标识、版本信息与中文边框布局

- 状态：已修复，待发布与本地验收
- 类型：显示缺陷 / 信息架构优化
- 范围：`hunter-harness` CLI 首页；`hunter-platform` 凭据校验响应
- 复现环境：Windows PowerShell，CLI `0.2.54`，已连接本地 Hunter Platform，CodeBuddy 通用配置。
- 现象：首页平台状态展示内部 `project_id` 而非项目名称；矩形面板右边框在中文状态行后错位并超出；首页没有显示当前 CLI 版本。

### 实施结果（0.2.55）

平台密钥信息接口会返回项目名称，`connect` 将其缓存到本地凭据；CLI 首页优先显示项目名称和实际执行版本，不再在主面板展示内部项目 ID。边框改为按中文、组合字符和 emoji 的终端显示宽度计算，并按 `COLUMNS` 对长地址和长名称换行。

### 根因与可行性

1. `project-menu.ts` 的平台状态只读取 `.harness/credentials.local.yaml`，其中当前仅保存 `project_id`，所以直接渲染了内部标识。平台仓库已有项目 `display_name`，项目详情 API 也会返回，但 `/api/v1/auth/key-info` 尚未返回项目名称。
2. `banner()` 使用 `[...line].length` 计算宽度。该值是 Unicode code point 数，不是终端显示单元格宽度；中文通常占两列。以当前平台状态形状测量，70 个 code point 实际占 78 个终端列，右侧 padding 少 8 列，因此边框稳定错位。实现也没有按 `stdout.columns` 限宽或对长 URL、长名称进行硬换行。
3. CLI 已有 `readCliVersion()`，可从实际安装包的 `package.json` 读取版本；首页没有调用它，不需要依赖服务端。

### 建议方案

- 平台的 `key-info` 对 project key 返回 `project_display_name`；该字段只描述密钥已经绑定的同一项目，不扩大权限范围。
- `connect` 缓存项目名称，首页优先显示实际项目名称；内部 ID 仅保留在“平台连接详情”、诊断或 JSON 输出中。旧凭据缺少名称时进行有超时的远端补全，失败则使用明确的离线/未同步提示，不阻塞本地主菜单，也不重新显示密钥。
- 首页建议显示为 `Hunter Harness v<cli-version> · <本地目录名>`，平台行单独显示 `平台：已连接 <url> · 项目 <display_name>`，避免把本地目录名与平台项目名混为一谈。
- 使用支持 CJK、组合字符和 emoji 的终端 cell-width 算法；先去除 ANSI 控制序列，再按终端列宽 padding。面板宽度应受当前终端列数约束，长 URL/项目名需要按显示单元格安全换行，不能仅把边框继续扩到视口外。

### 预期验收

1. 平台名称为中文、英文、emoji 或长字符串时，显示名称而非 `prj_*`；连接详情仍能查看 ID。
2. 40、80、120 列终端及无 TTY/未知列宽环境下，所有面板行的左右边框对齐且不超过可用宽度。
3. 覆盖中文、组合字符、emoji、长 URL、长无空格名称和 ANSI 样式文本的宽度测试。
4. 首页展示实际执行包版本，例如 `v0.2.54`；不得使用服务端版本或源码仓版本冒充当前 CLI 版本。
5. 旧版凭据、平台离线、名称补全超时或旧服务端不返回新字段时，主菜单仍能快速打开并给出可理解的降级状态。
6. 任何首页、错误或诊断输出都不得包含 API Key 明文。

## HH-MT-003：平台中文文案与版本记录视觉层级

- 状态：已修复，待发布与本地验收
- 类型：内容设计 / 显示缺陷
- 范围：`hunter-platform` Web 项目知识、全局知识中心、版本记录
- 现象：页面虽然默认使用中文，但主要操作和空状态仍混入 `push`、`ingest`、`purge`、`CLI`、“投影”“语义库”等实现术语，普通用户难以判断下一步该做什么；版本记录空状态把一整句说明显示成超大标题；项目知识页的“搜索”按钮在当前宽度下被挤成两行。

### 实施结果（0.2.55）

项目知识和全局知识中心的默认文案已改为“上传、整理、搜索、关系”等用户语言；版本记录使用独立标题、说明和数量徽标；项目知识与全局搜索按钮均禁止文字折行。对应空状态、标题层级和关系页签已有组件回归测试。

### 根因与判断

1. 默认语言本身没有配置错误：`I18nProvider` 首次加载使用中文，并只在用户主动选择后读取其本地语言偏好。问题在于中文文案直接暴露了后端流程和命令名，并非用户熟悉的任务语言。
2. 项目知识与全局知识中心对同一概念使用“项目知识”“知识库”“语义知识”“语义库”“ingest 投影”等多套叫法，增加了理解成本。页面应先说明用户能看到什么、需要做什么；协议和存储术语只应出现在可展开的诊断信息中。
3. 版本记录卡片在有数据时用 `<h2>` 显示版本数量，样式为 `1.8rem`；空状态复用了同一位置和样式来显示长说明句，因此字号异常。这是组件结构和标题语义复用错误，不是单纯的审美偏好。
4. 搜索按钮没有禁止换行，也没有可靠的最小宽度；输入区变窄后，两个汉字会纵向排列。

### 文案原则

- 中文为默认和第一呈现语言，英文继续作为可切换的第二语言；不根据浏览器语言自动把首次访问切到英文。
- 使用“上传、同步、整理、搜索、关联”等用户动作描述结果，不在主界面直接使用 `ingest`、`purge`、“投影”“语义库”等内部术语。
- 只有在用户确实需要输入某条命令时才保留 `push`、`CLI` 等原文，并同时解释用途；常规空状态不要求用户理解命令和处理管线。
- 空状态分为“发生了什么”和“下一步怎么做”两层；故障诊断与正常引导分开，避免一句话塞入多种分支。
- 同一概念在项目页、全局页、按钮、统计项和空状态中使用同一名称。

### 建议文案

| 位置 | 当前文案 | 建议文案 |
| --- | --- | --- |
| 项目知识说明 | 从项目文件与 ingest 投影建立的可检索知识与关系。 | 集中查看项目规则、变更总结和可搜索的知识内容。 |
| 导出按钮 | 导出上下文 | 导出项目知识 |
| 统计项 | 索引文档 / 知识 / 关系 | 已收录内容 / 知识条目 / 关联关系 |
| 关系页签 | 关系探索 | 知识关系 |
| 知识空状态标题 | 暂无项目知识。 | 还没有项目知识 |
| 知识空状态说明 | 来源包括 push 文件索引，以及 knowledge ingest 投影…… | 通过 Hunter Harness 上传项目内容后，平台会自动整理知识。处理完成后可在这里搜索和查看。 |
| 处理中提示 | 待投影 {n} 条 ingest 条目，语义库可能暂时不完整。 | 还有 {n} 条内容正在整理，结果可能暂时不完整。 |
| 版本记录说明 | 每次保存或 push 产生一个版本；展开可查看相对上一版的文件变更。 | 每次保存或同步文件后，平台都会保留一个版本。展开版本可查看具体改动。 |
| 版本空状态 | 保存第一个文件后，这里会出现版本记录。 | 还没有版本记录。保存或同步第一个文件后，版本会显示在这里。 |

最终文案需结合实际入口校正“上传”或“同步”的动作名称，避免页面引导一个当前并不存在的操作。

### 预期实现与验收

1. 项目知识与全局知识中心统一完成中文文案梳理；正常浏览路径不出现未解释的 `ingest`、`purge`、“投影”“语义库”等内部词汇。
2. 技术错误仍保留可诊断信息，但默认先显示中文原因和可执行建议，原始错误码或协议术语放在详情中。
3. 版本记录卡片使用正常字号的“版本记录”标题和正文说明；版本数量作为独立统计值或徽标，不再与说明文字共用大标题样式。
4. 空状态有清晰标题和一条简短行动建议；桌面端、窄屏和 200% 缩放下无溢出、遮挡或异常大字。
5. 搜索按钮保持单行且可点击，不挤压到纵向排列；输入框和按钮在窄屏下采用合理换行或堆叠布局。
6. 中英文词条结构保持一致，语言切换后不出现缺失键、退回另一种语言或布局跳变。
7. 为项目知识空状态、版本记录空状态和窄屏搜索栏补充组件测试或视觉回归测试。

## HH-MT-004：分支监控只产生本地事件，未形成实时远端上报闭环

- 状态：已修复，待发布与本地验收
- 类型：功能缺口 / 可靠性缺陷
- 范围：`hunter-harness` 事件写入与同步；`hunter-platform` Run 接收、状态投影与 Web 实时展示
- 复现项目：`E:\MyProject\AI Related\hunter-harness-test`，change `pomodoro-timer`，只完成 `plan` 阶段。
- 现象：本地已经产生运行事件，但平台“分支监控”仍为 0，页面需要手动刷新且没有说明本地是否正在上报、积压或失败。

### 实施结果（0.2.55）

事件在本地完成加锁、追加和 fsync 后会异步唤醒项目级有限同步进程；进程按最多 100 条分批上传，使用耐久游标，单条 ID 冲突进入不含原始敏感内容的隔离记录，不再阻塞后续事件。同步保留规范的 `timestamp` 和 `schema_version`。平台页面每 3 秒发现新增运行，SSE 断线后切换 REST 轮询；服务端按最近心跳动态返回在线、延迟或离线状态。

### 现场证据

1. 本地 `.harness/changes/pomodoro-timer/events.ndjson` 已有 10 条同一运行的 `plan` 事件，包含阶段开始、决策、产物、阶段结束和验证；因此不是 Agent 没有记录进度。
2. 本地不存在 `meta/events-sync-cursor.json` 或其他上传回执，说明同步器从未确认任何一行。
3. 平台 PostgreSQL 中该项目的 `runs=0`、`run_events=0`；服务端日志只有页面发出的 `GET /runs`，没有 `POST /runs/events:batch` 或 heartbeat。空页面忠实反映了服务端无数据，不是查询了错误项目。
4. 当前项目 API Key 有 `progress:write` 且未吊销，排除了权限缺失导致的上报失败。

### 根因

当前实现只有“同步工具”，没有完整的自动实时同步生命周期：

- `hunter-harness events-sync` 和服务端批量接收接口已经存在。
- 自动调用只位于 `harness_gate.py begin` 与 archive finalize。`harness-plan` 通过 context 流程直接写入 `phase.start/end`，不会调用 gate begin，因此整个规划阶段零上传。
- 后续阶段也只会在阶段开始时同步一次；阶段执行中新增的命令、验证、问题和阶段结束事件，要等下一个阶段开始或最终归档才会出现。这不属于实时监控。
- 每条本地事实耐久写入后没有 nudge、后台同步会话或重试调度；网络失败也没有面向用户的积压状态。

另有三个会让“手动同步”也不可靠的契约缺陷：

1. 本地事件使用 `timestamp`、`schema_version`，同步器白名单和时间映射却读取 `ts`、`schemaVersion`。当前已安装脚本与源码 SHA-256 完全一致；实测首条事件清洗后丢失这两个字段，`occurred_at` 回退成上传时刻，时间线会失真。
2. 服务端每批最多接收 500 条，客户端却把游标后的全部事件一次发送；长运行超过 500 条后会稳定失败并永久积压。
3. 游标只有在整批全部 accepted/duplicate 时才前移，缺少逐项 ACK、隔离和缺口修复；单个冲突或坏事件会阻塞后续所有合法事件。

### 远端展示侧缺口

- `RunsMonitor` 只在进入项目或点击刷新时拉取运行列表。列表为空时没有已选 Run，也就不会建立 SSE；页面无法自动发现稍后创建的第一条运行。
- 当前 SSE 是单 Run 订阅，不覆盖项目级“新增 Run”事件。
- SSE 建立后若异步断开，回调只记录错误，代码注释所说的 REST polling 并未真正启动；页面可能继续显示“实时”但不再更新。
- heartbeat 只把连接状态写为 `online`，服务端没有根据超时转成 `delayed/offline`，因此连接状态不能反映真实在线情况。
- 服务端 SSE 没有明确的保留游标下限、`resync_required`、慢消费者边界与客户端指数退避重连契约。

### 参考项目结论

`E:\WorkProject\kld-sdd` 与 `E:\WorkProject\kb-sdd` 已形成更完整的同类闭环，值得复用其边界而不是逐文件照搬：

1. 本地事件先原子、耐久地追加到 JSONL outbox；业务流程不等待网络。
2. 每次耐久追加后只 nudge 并唤醒一个用户级单实例、有限会话 Sync Agent，不常驻扫描源码，也不要求用户手动执行同步。
3. Sync Agent 公平扫描已注册工作区，约 500 ms 或达到阈值后批量发送；限制单事件、事件数和总字节。
4. 本地持久化逐事件 ACK、生产者序号、缺口、隔离项和退避状态；断网、响应丢失、进程重启后可安全重传，鉴权失败暂停对应工作区但不删除 outbox。
5. 服务端事务内幂等接收、更新投影并返回 ACK；REST 快照与带游标 SSE 配合，断线、游标过期或慢消费者时要求客户端重新获取快照。
6. Agent 在终态确认、干净空闲或重试预算结束后自行退出；下一条本地事实会再次唤醒，不需要永久后台服务。

### 建议架构

Hunter Harness 应采用相同思想的精简版本：

- 不做全仓文件监听。运行事件已经是可信事实源，应在 `harness_events.py` 完成加锁、追加和 fsync 后触发异步 nudge，避免扫描源码带来的噪声和资源消耗。
- `connect` 成功时注册工作区并验证 `progress:write`；每条事件落盘后唤醒用户级单实例同步会话。优先使用事件触发、空闲自动退出，不默认安装开机常驻服务。
- 同步器按项目和 Run 公平分批，使用稳定事件 ID 与序号、逐项 ACK、最大事件数/字节数、指数退避和有界重试；未 ACK 的核心事件不得自动删除。
- 保留 `npx hunter-harness events-sync --once/status` 作为诊断与手动恢复入口，但不能要求普通用户依赖它才能看到监控。
- 原始发生时间、阶段、attempt、执行工具和必要的中文摘要应按明确白名单上传；绝对路径、凭据、原始命令输出和其他敏感字段不得上传。
- 平台增加项目级 Run 变更流，或在无运行/存在 active run 时有界轮询列表；选中 Run 后再订阅详情流。SSE 断线应自动退避重连，游标失效时重新取快照。
- heartbeat 状态按服务端时间计算 `online → delayed → offline`，运行状态、连接状态和同步完整性继续保持三套独立状态。
- Web 空状态增加本机连接提示，例如“尚未收到运行记录”“本机有未同步事件”“最近上报失败”，但不得把密钥或本地绝对路径送到浏览器。

### 预期验收

1. 已连接项目执行 `harness-plan` 后，首条 `phase.start` 在正常本地网络下 3 秒内自动出现在已打开的平台页面，无需点击刷新或执行额外命令。
2. plan/run/test/review/submit/archive 全阶段的新增事件持续更新；阶段结束不会停留到下一阶段才显示。
3. 上传后保留事件真实时间和顺序，不以同步时间冒充发生时间；服务端和页面能区分 attempt。
4. 覆盖 501+ 事件、单项 schema 拒绝、重复事件、ID/序号冲突、部分 ACK、响应丢失和坏游标；合法后续事件不会被单个坏事件永久阻塞。
5. 平台离线一段时间后本地继续工作且事件不丢失；平台恢复时自动续传，不要求重跑业务阶段。
6. 同一用户多个项目并发、重复唤醒和 Agent/同步器崩溃时保持单实例或确定的项目隔离，不发生重复运行或串项目。
7. 页面打开时能自动发现新 Run；SSE 断开、服务重启、游标过期和慢消费者均能重连或重取快照，且不会错误显示“实时”。
8. heartbeat 超时后连接状态按阈值变成“延迟/离线”，但不把连接断开误判为运行失败。
9. 所有重试、状态文件、服务端日志和 Web 响应均通过敏感信息扫描，不包含 API Key、Authorization 或本地绝对路径。

## HH-MT-005：远端知识查询不接受项目 API Key

- 状态：已修复，待发布与本地验收
- 类型：权限契约缺口
- 范围：`hunter-harness` 远端 knowledge query；`hunter-platform` semantic search 鉴权与 API Key scope
- 现象：测试项目在 `harness-plan` 开始时按要求执行远端知识查询，收到 `project API keys cannot access this endpoint`，随后记录 `REMOTE_KNOWLEDGE_UNAVAILABLE` 并继续规划。

### 实施结果（0.2.55）

平台新增独立 `knowledge:read` 权限和项目级搜索端点；项目 Key 只能查询自身项目，全局或跨项目调用继续拒绝。CLI 查询已切换到项目路径，密钥页面、默认推荐权限、OpenAPI 和跨项目权限测试同步更新；已有 Key 不会静默扩权。

### 根因与判断

- CLI 绑定使用项目 API Key。当前可选 scope 只有 `push`、`knowledge:write`、`progress:write`、`files:read`，没有知识读取权限。
- `GET /api/v1/semantic/search` 调用未声明任何 project-key scope；鉴权层对项目 Key 默认拒绝，因此即使请求带精确 `project_id`，也必然返回 403。
- 这与“知识查询只调用远端、远端不可用时明确失败、不保留本地回退”的既定目标冲突。当前不是远端没有知识，而是合法 CLI 身份永远不能查询。

### 建议方案与验收

1. 新增显式 `knowledge:read` scope；不要把全局语义搜索权限隐式绑定到 `knowledge:write`。
2. 优先提供项目路径端点，例如 `GET /api/v1/projects/:projectId/semantic/search`；项目 Key 只能查询其绑定项目，跨项目或无 project filter 的搜索继续拒绝。
3. API 密钥页面、OpenAPI、CLI capability/connect 检查和中文权限说明同步更新。已有 Key 不静默扩权，应提示用户重新授权或采用可审计的迁移策略。
4. 正常查询、空结果、平台离线、权限缺失和跨项目访问返回不同的稳定错误码；CLI 不回退本地知识，也不把 403 或网络失败伪装成零命中。
5. 用真实项目 Key 覆盖“仅本项目成功、跨项目 403、全局搜索 403、用户会话仍可按权限全局搜索”的端到端测试。
